<form method="post" action="{{ path('categorie_audio_delete', {'id': categorie_audio.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_audio.id) }}">
    <button class="btn">Supprimer</button>
</form>
